import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  button: {
    borderRadius: 8,
    height: 50,
    width: 200,
    justifyContent: "center",
    alignItems: "center",
  },
  logo: {
    marginBottom: 64,
  },
});
